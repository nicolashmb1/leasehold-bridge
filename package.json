{
  "name": "leasehold-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Client-specific source adapter: Leasehold flat files → Propera financial snapshot contract",
  "type": "module",
  "scripts": {
    "export": "node src/cli/exportPropertySnapshots.js",
    "test": "node --test tests/*.test.js",
    "validate:ledger": "node scripts/validateLedgerMath.js",
    "validate:deposits": "node scripts/validateDepositFormula.js",
    "sync:changed": "node src/cli/syncChanged.js"
  },
  "engines": {
    "node": ">=18"
  }
}
